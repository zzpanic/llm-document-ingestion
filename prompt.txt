Transcribe this document page image to markdown. Extract everything visible — omit nothing.

---

PAGE METADATA (place at the very top of your output)
- If a page number is visible anywhere on the page, output: <!-- page: N -->
- If header text is visible (top margin, running title, document number, etc.), output: <!-- header: exact text -->
- Output both lines even if one is uncertain — guess rather than omit

---

BODY TEXT — pure transcription with minimal reformatting
- Preserve all heading levels exactly as they appear visually (#, ##, ###, ####)
- Reflow paragraph text into single unwrapped lines (remove hard line breaks within a paragraph)
- Preserve list hierarchy exactly (nested bullets and numbered lists)
- Preserve all footnotes and margin notes exactly as printed
- Convert dot-leader table-of-contents entries ("Section 4 ......... 12") into a markdown table with columns: Section | Page
- Do NOT add, infer, or summarise any content not visible on the page

---

MATHEMATICS AND SYMBOLS
- All equations, expressions, and formulae → LaTeX
  - Inline: $...$   |   Display/block: $$...$$
- All symbolic notation → LaTeX: subscripts $V_{in}$, Greek $\alpha$, units $\mu\text{A}$, operators $\leq$
- Never use Unicode for any symbol that has a LaTeX equivalent

---

TABLES
- Render as standard markdown tables with aligned columns
- Preserve all cell content including inline LaTeX

---

FIGURES AND DIAGRAMS
- Preserve the figure label and caption exactly as printed
- Immediately below the caption write a concise technical description:
  > **[Figure N]** Description for a reader who cannot see the image.
- For graphs: describe axes (labels, units, range), curve shapes, key data points, legend
- For schematics: describe topology, components, signal flow, labelled nodes
- Do not omit figures

---

FOOTER (place at the very bottom of your output)
- If footer text is visible (bottom margin, copyright, standard reference, etc.), output: <!-- footer: exact text -->

---

OUTPUT RULES
- Bare markdown only — no preamble, no commentary, no closing remarks
- No code fences wrapping the entire output
- No "Here is the markdown:" or similar framing
- This is one page of a multi-page document — do not add a page heading
- If the page begins mid-sentence, continue from that point without preamble
