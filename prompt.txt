Extract the image to clean markdown. Rules:

STRUCTURE
- Preserve all heading levels exactly (use # through ####)
- Reflow paragraph text; do not hard-wrap lines
- Convert "......." dot leaders in tables of contents to proper markdown tables
- Preserve list hierarchy (nested bullets/numbered lists)

HEADERS, FOOTERS & PAGE NUMBERS
- Capture all header and footer text exactly as it appears
- Mark headers as: 
- Mark footers as: 
- Mark page numbers as: 
- Place these markers at the top (header/page) and bottom (footer) of the page output
- Do not omit any marginal text — it will be used in post-processing

FIGURES & IMAGES
- For every figure, diagram, graph, chart, photo, or line drawing:
  1. Preserve the figure label and caption exactly as printed (e.g. "Figure 3: Frequency response of the filter")
  2. Immediately below the caption, write a detailed technical description in this format:
     > **[Figure N description]** Description text here.
- The description should be written for an LLM reader with no access to the image:
  - For graphs/charts: describe axes (labels, units, range), curve shapes, key data points, trends, and any annotations or legend entries; include values as LaTeX where symbolic (e.g. $f_c = 10\,\text{kHz}$)
  - For diagrams/schematics: describe the topology, components, signal flow, and any labelled nodes or pins
  - For photos/drawings: describe subject, orientation, and any visible labels or markings
- If there is no caption, infer a label from context (e.g. "Unlabelled figure") and still provide the description
- Do not omit figures — a missing figure description is worse than an imperfect one

MATHEMATICS & SYMBOLS
- ALL equations, expressions, and formulae → LaTeX
  - Inline: $...$
  - Display/block: $$...$$
- ALL symbolic notation → LaTeX, including:
  - Subscripts/superscripts: $V_{in}$, $x^2$, $T_{amb}$
  - Greek letters: $\alpha$, $\Delta$, $\omega$
  - Units with symbols: $\mu\text{A}$, $\Omega$, $\text{dB}$
  - Operators and relations: $\leq$, $\pm$, $\times$
- Never use Unicode for any symbol that has a LaTeX equivalent
- When uncertain whether something is a subscript or abbreviation, treat it as LaTeX

TABLES
- Render as standard markdown tables with aligned columns
- Preserve all cell content, including inline LaTeX

PAGE-CONTINUITY AWARENESS
- This is one page of a multi-page document
- Do not add any heading for the page itself
- If the page begins mid-sentence or mid-paragraph, continue from that point without preamble
- If a heading or paragraph appears cut off at the bottom, render what is visible and stop

OUTPUT
- Bare markdown only — no commentary, no preamble, no closing remarks
- No code fences around the output
- No "Here is the markdown:" or similar framing
